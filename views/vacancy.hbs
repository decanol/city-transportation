<form action="/vacancy/create" method="post">
    <h3>Create Record</h3>
    <div class="inputs">
        <div class="input-field">
            <input type="text" name="title_vacancy" class="validate" pattern="^[A-z]{1-3}$">
            <label>Title</label>
            <span class="helper-text" data-error="wrong" data-success="right"></span>
        </div>
    </div>

    <div class="inputs">
        <div class="input-field col s12">
            <textarea id="textarea1" class="materialize-textarea" name="content_vacancy"></textarea>
            <label for="textarea1">Requirements</label>
            <span class="helper-text" data-error="" data-success=""></span>
        </div>
    </div>
    <button type="submit" class="btn waves-effect waves-light" id="submitForm" disabled="disabled">Create
        <i class="material-icons right">add_box</i>
    </button>
</form>

<h3>Vacancies</h3>

{{#if vacancy.length}}
    <div class="news-list-adm">
    <ul>
        {{#each vacancy}}
                <li>
                    <h5>
                        {{title_vacancy}}
                    </h5>
                    <p>
                        {{content_vacancy}}
                    </p>
                    <a href="/vacancy/delete/{{_id}}" class="btn waves-effect waves-light delete-btn">Delete</a>
                    <a href="/edit-vacancy/{{_id}}" class="btn waves-effect waves-light delete-btn">Edit</a>
                </li>
        {{/each}}
    </ul>
    </div>
{{else}}
    <p>*table's empty</p>
{{/if}}
